import React from 'react'
import styles from '../../styles/index/Content.module.scss'

interface Props {}

const Content = ({}: Props) => {
  return <div className={styles.content}>Content index component</div>
}

export default Content
